<nav class="navbar navbar-expand-lg navbar-light bg-light ">
  <%= link_to "BookYourRoom", root_path, class: 'navbar-brand' %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <% if logged_in? %>
        <li class="nav-item active">
          <%= link_to "Search Spaces", hotels_path, class: 'nav-link' %>
        </li>
        <li class="nav-item">
          <%= link_to "Owned spaces", {controller: "users", action: "show_hotels", id: current_user.id}, class: "nav-link" %>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            Profile
          </a>
          <div class="dropdown-menu" aria-labelledby="navbarDropdown">
            <%= link_to "Show profile", user_path(current_user), class: 'dropdown-item' %>
            <%= link_to "Edit profile", edit_user_path(current_user), class: 'dropdown-item' %>
            <div class="dropdown-divider"></div>
            <%= link_to "Delete profile", '#', class: 'dropdown-item' %>
          </div>
        </li>
      <% else %>
        <li class="nav-item">
          <%= link_to "Login", login_path, class: 'nav-link' %>
        </li>
        <li class="nav-item">
          <%= link_to "Signup", signup_path, class: 'nav-link' %>
        </li>
      <% end %>
    </ul>
    <ul class="navbar-nav ml-auto">
      <% if logged_in? %>
       <li class="nav-item mr-2">
          <%= link_to current_user.name, user_path(current_user), class: 'nav-link' %>
        </li>
        <li class="nav-item mr-2">
          <%= link_to "Logout", logout_path,class: 'nav-link btn btn-warning', method: :delete %>
        </li>
      <% end %>
    </ul>
    <%# 
    <form class="form-inline my-2 my-lg-0">
      <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
      <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
    </form> %>
  </div>
</nav>